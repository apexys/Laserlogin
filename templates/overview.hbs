{{#*inline "page"}}
  
    <h2>User details:</h2>
    <table class="nostyle">
      <tbody id="rows">
        <tr><td>Email:</td><td>{{user.email}}</td></tr>
        <tr><td>Current project:</td>
        <td>
          <form method="POST" action="/user_update_project">
            <input type="hidden" name="userid" value="{{user.id}}">
            <input type="text" name="current_project" value="{{user.current_project}}" style="width: 10em;">
            <button style="font-weight: bold; margin-right: 0; margin-left: auto;">✔</button>
          </form>
        </td></tr>
      </tbody>
    </table>
    <h2>Logs:</h2>
    <table class="nostyle">
      <thead>
        <tr>
          <td>Date</td>
          <td>Duration</td>
          <td>Project</td>
        </tr>
      </thead>
      <tbody id="rows">
        {{#each logs}}
          <tr>
            <td>{{date}}</td>
            <td>{{duration}}</td>
            <td>{{project}}</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
   
{{/inline}}
{{~> base~}}